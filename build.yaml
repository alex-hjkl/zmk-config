# `nice_nano` is the current nice!nano v2 default revision in ZMK.
# If your controllers are nice!nano v1, change both entries to `nice_nano@1`.
---
include:
  - board: nice_nano
    shield: corne_left
  - board: nice_nano
    shield: corne_right
