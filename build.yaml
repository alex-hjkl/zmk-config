# Use the plain nice!nano board and provide Corne underglow via local overlays.
# If your controllers are nice!nano v1, change both entries to `nice_nano@1`.
---
include:
  - board: nice_nano
    shield: corne_left
  - board: nice_nano
    shield: corne_right
