# Use the ZMK board variant on current main so BLE/battery features stay enabled.
# PinMicro clones can behave closer to nice!nano v1 than the current v2 default.
---
include:
  - board: nice_nano@1//zmk
    shield: corne_left
  - board: nice_nano@1//zmk
    shield: corne_right
