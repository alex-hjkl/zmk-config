# Pinned to ZMK v0.3, before the Zephyr 4.1 board variant migration.
# Keep the plain nice!nano board to match the user's previously working setup.
---
include:
  - board: nice_nano
    shield: corne_left
  - board: nice_nano
    shield: corne_right
